# leader101git init
git add index.html
git commit -m "feat: deploy premium bilingual leadership self-study suite with core fixes"
git remote add origin <your-github-repository-url>
git branch -M main
git push -u origin main
3. Once pushed, navigate to your repository settings on GitHub, select **Pages** from the sidebar, set the build source to **Deploy from a branch (main / root)**, and click Save. Within minutes, your interactive executive suite will be live on your custom `*.github.io` domain!

